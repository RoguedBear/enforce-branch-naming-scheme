{
  "name": "enforce-branch-naming-scheme",
  "version": "1.0.0",
  "description": "Github action to enforce naming convention on branch names",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "ncc build index.js --license licenses.txt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "actions"
  ],
  "author": "deepakputhraya",
  "contributors": ["RoguedBear"],
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
